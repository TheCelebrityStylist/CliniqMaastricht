import Link from 'next/link'
import { site } from '@/lib/site'
import InquiryForm from '@/components/forms/InquiryForm'
import { cmsMetadata } from '@/lib/pageMetadata'
export async function generateMetadata() { return cmsMetadata('contact', 'nl') }
export default function ContactPage(){return <section className="container-premium pt-36 pb-24"><p className="eyebrow">Contact</p><h1 className="h1 mt-5">Neem contact op.</h1><div className="mt-12 grid gap-8 lg:grid-cols-2"><div className="space-y-6"><div className="card rounded-[2rem] p-8"><h2 className="h3">Cliniq Maastricht</h2><p className="mt-4 text-white/70">{site.address.street}<br/>{site.address.postalCode} {site.address.city}</p><p className="mt-4"><a href={`tel:${site.phone}`}>{site.phone}</a><br/><a href={`mailto:${site.email}`}>{site.email}</a></p><div className="mt-6 flex gap-3"><Link className="btn-primary" href="/uitgaan">Agenda</Link><Link className="btn-secondary" href="/event-space">Ruimte huren</Link></div></div><div className="card overflow-hidden rounded-[2rem]"><iframe title="Route naar Cliniq Maastricht" src={site.maps} className="h-80 w-full border-0" loading="lazy"/></div></div><InquiryForm type="contact" fields={[{name:'name',label:'Naam',required:true},{name:'email',label:'E-mail',type:'email',required:true},{name:'phone',label:'Telefoon'},{name:'message',label:'Bericht',required:true}]} /></div></section>}
