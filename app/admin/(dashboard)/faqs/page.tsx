import { readStore } from '@/lib/admin/store'
import { saveFaqAction } from '@/lib/admin/actions'

export default async function FaqAdminPage() { const store=await readStore(); return <div className="grid gap-8 xl:grid-cols-[1fr_420px]"><section><p className="text-xs font-black uppercase tracking-[0.28em] text-[#f02688]">Conversion content</p><h1 className="mt-3 text-5xl font-black tracking-[-0.05em]">FAQ Manager</h1><div className="mt-8 grid gap-3">{store.faqs.sort((a,b)=>a.pageKey.localeCompare(b.pageKey)||a.order-b.order).map(faq=><details key={faq.id} className="rounded-[1.5rem] bg-white p-5"><summary className="cursor-pointer font-black">{faq.pageKey} · {faq.language.toUpperCase()} · {faq.question}</summary><p className="mt-3 leading-7 text-black/65">{faq.answer}</p></details>)}</div></section><aside className="rounded-[2rem] bg-white p-6"><h2 className="text-2xl font-black">Add detailed FAQ</h2><form action={saveFaqAction} className="mt-6 grid gap-4"><Select name="pageKey" label="Page" options={store.pages.map(p=>[p.key,p.titleNl])}/><Select name="language" label="Language" options={[["nl","Nederlands"],["en","English"]]}/><Field name="question" label="Question"/><Area name="answer" label="Detailed answer"/><Field name="order" label="Order"/><button className="rounded-full bg-[#f02688] px-5 py-3 text-sm font-black uppercase tracking-[0.14em] text-white">Save FAQ</button></form></aside></div> }
function Field({ name,label }: { name:string; label:string }) { return <label className="grid gap-2 text-sm font-bold">{label}<input name={name} className="rounded-2xl border border-black/10 bg-white px-4 py-3" /></label> }
function Area({ name,label }: { name:string; label:string }) { return <label className="grid gap-2 text-sm font-bold">{label}<textarea name={name} rows={7} className="rounded-2xl border border-black/10 bg-white px-4 py-3" /></label> }
function Select({ name,label,options }: { name:string; label:string; options:string[][] }) { return <label className="grid gap-2 text-sm font-bold">{label}<select name={name} className="rounded-2xl border border-black/10 bg-white px-4 py-3">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label> }
