import { readStore } from '@/lib/admin/store'
import { saveSettingsAction } from '@/lib/admin/actions'

export default async function SettingsAdminPage() { const store=await readStore(); const s=store.settings; return <section className="max-w-3xl rounded-[2rem] bg-white p-6"><p className="text-xs font-black uppercase tracking-[0.28em] text-[#f02688]">Global</p><h1 className="mt-3 text-5xl font-black tracking-[-0.05em]">Site Settings</h1><form action={saveSettingsAction} className="mt-8 grid gap-4"><Field name="phone" label="Phone" defaultValue={s.phone}/><Field name="email" label="Email" defaultValue={s.email}/><Field name="whatsapp" label="WhatsApp URL" defaultValue={s.whatsapp}/><Field name="address" label="Address" defaultValue={s.address}/><Field name="instagram" label="Instagram" defaultValue={s.instagram}/><Field name="tiktok" label="TikTok" defaultValue={s.tiktok}/><label className="grid gap-2 text-sm font-bold">Opening hours<textarea name="openingHours" defaultValue={s.openingHours.join('\n')} rows={7} className="rounded-2xl border border-black/10 bg-white px-4 py-3" /></label><button className="w-fit rounded-full bg-[#f02688] px-5 py-3 text-sm font-black uppercase tracking-[0.14em] text-white">Save settings</button></form></section> }
function Field({ name,label,defaultValue }: { name:string; label:string; defaultValue?:string }) { return <label className="grid gap-2 text-sm font-bold">{label}<input name={name} defaultValue={defaultValue||''} className="rounded-2xl border border-black/10 bg-white px-4 py-3" /></label> }
